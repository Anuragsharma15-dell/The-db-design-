services:
  - type: web
    name: database-schema-designer
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: apiKey
        sync: false
    healthCheckPath: /
    autoDeploy: true
